<%= 'Project: ' %>
<%= select_tag 'settings[project]', options_for_select(Project.all.collect{ |u| [u.name, u.id] }, settings['project']) %>
<br>
<%= 'Assignee: ' %>
<%= select_tag 'settings[user_assignee]', options_for_select(User.all.collect{ |u| [u.lastname, u.id] }, settings['user_assignee']) %>
