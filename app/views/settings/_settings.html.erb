<%= form_tag({:controller => 'settings', :action => 'plugin', :id => 'my_plugin'}, :method => :post) do %>
  <%= select_tag 'priority', options_for_select([['High', 'high'], ['Medium', 'medium'], ['Low', 'low']]) %>
  <%= submit_tag 'Save' %>
<% end %>

<%= form_tag({:controller => 'settings', :action => 'plugin', :id => 'my_plugin'}, :method => :post) do %>
  <%= select_tag 'priority', options_for_select([['High', 'high'], ['Medium', 'medium'], ['Low', 'low']]) %>
  <%= submit_tag 'Save' %>
<% end %>